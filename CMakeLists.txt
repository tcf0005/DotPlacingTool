cmake_minimum_required(VERSION 2.8)
project( DPT )                                 #replace test
find_package( OpenCV REQUIRED )
add_executable( DPT DPT.cpp )                 #replace test and test.cpp
target_link_libraries( DPT ${OpenCV_LIBS} )    #replace test

